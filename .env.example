# Google Drive Integration (Service Account - NOT API Key)
# To enable real Google Drive uploads, follow these steps:

# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select existing one
# 3. Enable Google Drive API
# 4. Create a Service Account (NOT an API key)
# 5. Download the service account JSON credentials
# 6. Fill in the values below from your service account JSON

GOOGLE_PROJECT_ID=your-project-id
GOOGLE_PRIVATE_KEY_ID=your-private-key-id
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-private-key-here\n-----END PRIVATE KEY-----\n"
GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_CLIENT_ID=your-client-id

# Optional: Specify a Google Drive folder ID to upload files to a specific folder
# If not provided, files will be uploaded to the root of the service account's Drive
GOOGLE_DRIVE_FOLDER_ID=your-folder-id

# Database
MONGODB_URI=mongodb://localhost:27017/examtopics

# JWT Secret
JWT_SECRET=your-jwt-secret-key-here

# Environment
NODE_ENV=development
